# Disable default grid and header components.
--style=plain,changes,numbers

# Always use colors.
--color=always

# Wrap with maximum width of 120
--wrap=auto
--terminal-width=120

# Never use a pager utility.
--paging=auto

# Use 2 length spaced tabs.
--tabs=2